/**
 * @jsx React.DOM
 */

'use strict';

var React = require('react/addons');

var LoginPage = React.createClass({
  render: function() {
    return (
      <div className='main'>
        Login page
      </div>
    );
  }
});

module.exports = LoginPage;
